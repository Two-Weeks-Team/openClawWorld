{
  "name": "@openclawworld/client",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "tsc && vite build",
    "clean": "rm -rf dist",
    "dev": "vite",
    "lint": "eslint src --ext .ts",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "preview": "vite preview"
  },
  "dependencies": {
    "@colyseus/schema": "^4.0.14",
    "@colyseus/sdk": "0.17",
    "@openclawworld/shared": "workspace:*",
    "phaser": "^3.90.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vite": "^7.3.1",
    "vitest": "^4.0.18"
  }
}
